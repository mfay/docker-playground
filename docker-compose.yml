version: '2'
services:
  proxy:
    build: ./proxy
    image: mfay/haproxy:1.1 
    ports:
      - "80:80"
      - "70:70"
    links:
      - weba 
      - webb 
      - webc 
    restart: always
  weba:
    build: ./web 
    restart: always
  webb:
    build: ./web 
    restart: always
  webc:
    build: ./web 
    restart: always
